services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tracker
      POSTGRES_PASSWORD: tracker
      POSTGRES_DB: habit_expense_tracker
    ports:
      - "5432:5432"
    volumes:
      - tracker_pgdata:/var/lib/postgresql/data

volumes:
  tracker_pgdata: